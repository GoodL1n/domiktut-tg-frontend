import { FormatTextToNumberPipe } from './format-text-to-number.pipe';

describe('FormatTextToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTextToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
